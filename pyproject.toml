[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "quantum-rag"
version = "0.1.0"
description = "A small, local, hybrid RAG tool for grounding technical conversations in your own PDFs and notes"
readme = "README.md"
license = { text = "MIT" }
authors = [{ name = "Salvatore Pennacchio" }]
requires-python = ">=3.9"
dependencies = [
    "PyMuPDF>=1.24",
    "numpy>=1.26",
    "scikit-learn>=1.4",
    "sentence-transformers>=3.0",
]

[project.urls]
Homepage = "https://github.com/tatopenn-cell/quantum-rag"
Repository = "https://github.com/tatopenn-cell/quantum-rag"

[project.scripts]
quantum-rag-build = "build_index:main"
quantum-rag-query = "query:main"

[tool.setuptools]
py-modules = ["build_index", "query"]
